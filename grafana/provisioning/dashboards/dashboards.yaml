apiVersion: 1

providers:
  - name: 'DefaultProvider'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    allowUiUpdates: true  # UIでの更新を許可
    updateIntervalSeconds: 60  # 更新間隔を1分に設定
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: true